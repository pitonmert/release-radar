services:
  release-radar:
    build: .
    container_name: release-radar
    restart: unless-stopped
    stop_grace_period: 2m
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/release_radar.db
    volumes:
      - ./data:/app/data
      - ./config.json:/app/config.json:ro
